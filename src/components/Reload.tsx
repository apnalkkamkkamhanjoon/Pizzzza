import React from 'react';
import { GrPowerReset } from "react-icons/gr";
import '../styles/reload.scss'

const Reload = () => {
    return (
        <div>
            <GrPowerReset className='reload'/>
        </div>
    );
};

export default Reload;